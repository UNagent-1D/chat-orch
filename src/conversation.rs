// Task 10: Full implementation pending.
//
// This module contains the core conversation turn logic.
// It takes a ResolvedMessage + Session + AgentConfig, runs the LLM turn loop,
// and returns an AgentResponse.
//
// This is a single file — not a module directory — because the logic is
// orchestration glue between session, config, and llm. If it grows past
// 300 lines, promote to a directory module.
