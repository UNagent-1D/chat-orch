// Task 4: Full implementation pending.
//
// Domain types for the orchestrator. Each type lives in its own file.
// The TypeState pattern is enforced: IngestMessage → ResolvedMessage.

pub mod agent_response;
pub mod ingest_message;
pub mod message_content;
pub mod resolved_message;
pub mod session;
